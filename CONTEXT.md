# 多租户企业应用

租户边界以组织为单位。Projects 是组织内的项目管理参考领域。以下术语区分组织是否在运营，以及内容的显示语言与成员的编辑权限。

## Language

### 组织

**组织状态**：
组织当前是否在运营，取值为 ACTIVE 或 SUSPENDED。缺失不等于可用。
_Avoid_：运行状态, 运行时状态, OrganizationRuntimeStatus, enabled

**ACTIVE**：
组织处于运营中；成员可进行该组织的受保护业务与管理操作。
_Avoid_：启用, enabled

**SUSPENDED**：
组织已停止运营；成员不能进行该组织的受保护读写，仍可看到最小组织列表并切换到其他 ACTIVE 组织。
_Avoid_：禁用, disabled

**停用**：
把组织状态改为 SUSPENDED 的动作。
_Avoid_：关闭, 删除组织

**恢复**：
把组织状态改为 ACTIVE 的动作。不重建 Membership、不恢复已撤销权限、不延长邀请。
_Avoid_：启用, 重新开通

**工作区偏好**：
会话中的 activeOrganizationId，只表明访问目标，不证明授权。
_Avoid_：授权组织, 当前租户凭据

### 项目与内容语言

**项目（Project）**：
属于一个组织的业务资源，具有状态及不同语言的名称、描述。

**界面语言**：
按钮、提示及日期等本地化展示所采用的语言设置，不决定正在编辑的内容语言或成员权限。
_Avoid_：项目语言、内容语言

**内容语言**：
一份项目名称与描述所属的语言，也是编辑内容时明确选择的目标语言；支持中文（zh-CN）、英文（en-US）和阿拉伯文（ar）。
_Avoid_：界面语言

**项目基准内容语言（contentLocale）**：
项目创建时确定的内容语言，用于在请求语言没有译文时选择显示内容，首版创建后不可修改。它不赋予对应语言特殊编辑权限，也不随界面语言或组织默认语言的变化而改变。
_Avoid_：当前界面语言、组织默认语言、原文专属编辑权限

**项目译文**：
项目在某一种内容语言下的名称与描述，描述可以为空。

**基础译文**：
项目基准内容语言对应的项目译文，在项目存续期间必须存在。“基础”表示内容显示时的基准，不表示独立的编辑权限。
_Avoid_：只能由项目更新权限修改的原文

**组织默认语言（defaultLocale）**：
组织的默认语言设置，也是创建项目时未明确指定项目基准内容语言时的取值来源。修改它不改变既有项目的基准内容语言。
_Avoid_：项目基准内容语言、当前界面语言

**项目更新权限（project:update）**：
允许修改项目状态及任意受支持语言的名称、描述的组织级权限；维护内容不额外要求项目翻译权限。
_Avoid_：仅状态修改权限、仅基础语言编辑权限

**项目翻译权限（project:translate）**：
允许维护任意受支持语言的名称、描述，包括基础译文的组织级内容维护权限；仅持有此权限不能修改项目状态。
_Avoid_：仅非基础语言编辑权限、自动翻译能力
